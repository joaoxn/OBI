#include <bits/stdc++.h>
using namespace std;

int t, x;

int main() {
    scanf("%d", &t);
    for (int _t = 0; _t < t; ++_t) {
        scanf("%d", &x);
        printf("%d\n", x);
    }

    return 0;
}